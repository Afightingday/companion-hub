# 雨施流形 · 私人线条动画交接

## 1. 任务目标

为「雨施流形」设计一套私人、艺术化的抽象线条动画，供 AI 的 Thinking 与工具调用场景使用。

本分支只负责把动画效果本身设计、实现并展示出来。它是一个独立的 SwiftUI 动画实验室，不负责把动画接入聊天、Gateway 或 ProviderEvent。

## 2. 仓库与分支

- 私仓源码：`madebyyouyou/yushi-liuxing`（2026-07-20 账号改名，原 `lulu-jun/…`；旧地址 GitHub 会重定向，但请用新地址）
- 本地项目根目录：`C:\Users\batian\Projects\yushi`
- 私仓工作分支：`codex/line-animation-study`
- 私仓分支起点：提交 `e3eb3d1`
- 公仓 CI 镜像：`Afightingday/companion-hub`
- 公仓镜像分支：`codex/line-animation-study`
- 最低系统：iOS 26
- 当前用户没有 Mac，最终视觉验收由公仓 GitHub Actions 的 macOS Runner 完成。

所有动画源码只在私仓 `apps/ios` 中创作和提交。公仓同名分支由 `apps/ios/sync-to-public.ps1` 重建，只负责 macOS CI；不要直接在公仓修改动画源码。

## 3. 已确认的视觉方向

### 3.1 基本形式

- Thinking 与工具调用是两类相互独立的线条动画。
- 不同工具拥有不同动画，不使用统一的扳手、齿轮或加载圈。
- 工具动画尺寸为 **20–28 pt**，用于状态文字旁的小图标位置。
- **例外：`Thinking｜起草` 走 36–56 pt 的大尺寸场景**（2026-07-20 用户拍板）。实测（接触表 `dpr-study`，按 @1x/@2x/@3x 设备像素渲染）：「祐」九笔密集，20 pt 三竖粘连、24–28 pt 临界并笔，**36 pt 起才笔笔分明**。不为迁就小图标位而简化字形。展板已按 `MotionSpec.previewSizes` 分档，起草单列 36/44/56 pt。
- 动画存在感为「适中」：一眼能看出线条正在运动，但不能抢聊天正文的注意力。
- 整体气质为：**墨线的质感 × 雨水的运动 × 星轨 / 流形的结构**。
- **动画要能让人自然联想到对应功能**（2026-07-18 用户确认，取代旧表述「不要求猜出功能」——纯抽象线条被否，理由：太抽象、毫无意义）；但要用独特、有设计感的隐喻表达，功能仍由旁边文字兜底。
- 不使用放大镜、时钟、计算器、文件夹、代码括号等通用功能标识。

### 3.2 颜色

- 主体统一使用项目的抹茶深墨色。
- 不同动画只在关键节点短暂出现微弱个性色。
- 可用的点缀方向：腮红、雨蓝、暖金。
- 色彩不是工具分类标签，只是运动中的短暂情绪。

### 3.3 首批动画组

1. Thinking
2. 搜索
3. 网页读取
4. 时间
5. 计算
6. 文件
7. 代码 / 命令
8. 记忆
9. 未知工具兜底

「计算」保留的原因：现有 Gateway 内置了 `calculator`，用于可靠执行算术表达式，避免模型心算错误。但动画本分支不接入该工具，只设计对应视觉。

## 4. 已确认的动画隐喻

### Thinking｜起草（2026-07-20 v7：轨迹由位图骨架反推）

**架构（蒙版揭示,v5 定;素材 v6 换用户 PSD 分层;轨迹 v7 改为反推）**：墨迹成品 = 用户分层的 **9 笔位图**（`design-refs/you_oracle_fixed_canvas_package/`,画布 1052×1252,**层号即笔顺**:上横→下横→中竖→左竖→右竖→横折→长竖钩→匣→匣内横;打包进 `MotionLab/GlyphStrokes/*.png`）。**每层蒙版只揭示本层,零渗漏**;字形按九层联合墨盒 (165,151,723,1024) **居中填满 glyphBox**。位图以 destinationIn 染墨,适配纸/深双底。隐藏轨迹仅驱动笔位/笔顺/蒙版。钢笔**加粗 ×1.6、放大 0.40**,固定倾角 38° 纯平移。单字循环:逐笔揭示 → 悬笔端详 → 墨沉消散 → 回笔蓄势。

**v9 去机械感（2026-07-20 用户反馈「还是感觉动画很硬」）**

用户给了三条诊断，均已处理；另加两项来自运动控制文献的做法。**注意：此轮无法联网检索（工具配额受限），以下依据是既有知识而非当场查证的资料，采纳前值得复核。**

- **① 笔角固定 → 笔杆倾靠**：`penAngle` 之上叠加随行笔方向的偏转（以右下 45° 为中性，幅度上限 `leanMax` 15°），并对过去 `leanLag`(0.055) 窗口做指数加权平均形成**滞后**——即动画十二原则里的跟随/重叠动作：附属部件（笔杆）转向要慢于主体（笔尖）。`draw` 是相位的纯函数、不能用有状态滤波器，故就地按窗口积分。悬笔阶段倾靠缓缓回正，不一撒手就弹直。
- **② 笔画间没呼吸 → 分级停顿**：间隙不再是清一色的 `strokeGap`，改乘 `gapScale`。祐 = 礻(1–5) + 右，右 = 𠂇(6–7) + 口(8–9)，**部件边界处换气**：5→6 为 2.4×、7→8 为 1.9×，笔画内 0.9–1.35×。实际时长 0.11–0.31s（v8 是清一色 0.05s）。
- **③ 速度 → 0.4×**：`cycle` 4.4s → **11.0s**（用户定）。
- **曲率控速（two-thirds power law）**：人画曲线时 `v ∝ κ^(-1/3)`，弯得越急走得越慢。**沿弧长匀速推进正是「机械感」的数学根源**。按等弧长采样点的曲率把时间重新分配到弧长，速度比夹在 `[1/1.40, 1.40]` 内，免得直线段飙速、拐角处停死。
- **等时性（isochrony）**：笔越长写得相对越快，故时间片 ∝ 长度^0.65 而非 ∝ 长度。
- **调参留痕**：曲率扭曲单独 0.79–2.13×、整笔缓动单独 0.26–1.72×，**两者相乘会冲到 3.4×**（过火）。故把曲率幅度收到 1.40、缓动软化为 `(0.45,0.30,0.45,0.75)`，复合后 0.40–2.57×。峰值高于 v8 的 2.10×，但性质不同——**快在直线段、慢在拐角**，这才是人写字的特征。

**v8 版面与书写节奏（2026-07-20 用户反馈）**：

- **版面**：字形原先占容器 43.8%×62%，且左留白 0.176 / 右 0.386，整整**偏左 10.5% 容器宽**。现 `glyphBox = (0.2008, 0.10, 0.5648, 0.80)`——字高占 80%、字宽 56.5%（面积 +66%）。水平按**墨重心**居中：右侧「右」比左侧「礻」重 19.8%，纯外框居中会显得偏右，故据实测重心（union 内 0.5298）左移 0.0168；垂直按外接框居中——竖直重心偏上（0.4249），若也按重心校正会把字压到底边只剩 0.04 留白，属过度校正。常数由 `tools/motion-sketch/measure-glyph.mjs` 实测推导，不是估的。
- **书写节奏**：单笔拆成**落笔 → 行笔 → 收笔**三段（`downFrac` 0.08 / `upFrac` 0.10）。落笔段墨不动、笔从 `landHeight` 降到纸面；行笔段墨随三次贝塞尔 `(0.40, 0.18, 0.40, 0.85)` 推进（峰值 1.72× 均速、起速 46%、收速 26%）；收笔段墨已满、笔提起并沿出锋方向带出。笔画间飞渡自上一笔的收笔位落到下一笔的落笔悬停位，两端 `sin` 归零故接缝连续。**曾试 Material `(.40,0,.20,1)`，峰值 2.73× 且两端归零，配上静止段反而更顿挫，弃用。**
- **要害**：`strokeRhythm` 的 `prog` **同时驱动蒙版揭示与笔位**。历史上这两处是各算各的；一旦分家，笔就会脱离墨的前沿。改动这一带时务必保持同源。
- **笔允许短暂出画**（用户 2026-07-20 明确：不要为迁就笔而牺牲字形）。当前笔身最大探出约 22.5% 容器边长。

**v7 修复（用户反馈第 6 笔断裂）**：v6 的 9 条 median 是照字形结构**手写**的，从未与位图配准。第 6 笔（反 C 形横折）轨迹**反向且横穿空腔**，蒙版扫过空腔无墨可揭 → 一笔裂成两段、时间片结束才跳补；早现的那截孤墨其实是它自己的下横（恰在第 7 笔起始横段的高度上，故被误读为「第 7 笔提前显现」——第 7 笔图层实际从未提前绘制）。第 8 笔也查出实为 **U 形开口向上，非闭合方框**。

现改为**从位图 alpha 反推中心线**：Zhang-Suen 细化取骨架 → 最大连通分量的**图直径（双向 BFS）**作中心线（比找度为 1 的端点稳健：粗笔细化后常无度 1 端点，会被误判成闭环）→ 按弧长重采样 → 首末沿切向各外延 0.8×笔半径。蒙版宽 = 2×笔半径（距离变换 85 分位）×1.4375，比手写值收窄近半，揭示前沿更贴笔尖。**验收指标**：把走完全程的蒙版栅格化，统计该层墨的未覆盖比例，须 ≤0.2%（现 8 笔为 0、最差第 6 笔 0.18%），否则收尾会跳补。

### 搜索｜拾音（2026-07-18 v3 概念 + 2026-07-19 v4 构图修正）

**并行的雨丝被风吹斜**（无圆头、极细、错峰坠落），落在一条看不见的水面上：众丝落水各起一圈极淡墨色微澜，唯命中那根减速落定，**荡开雨蓝双圈椭圆涟漪**（椭圆即水面透视），随后没入水面。v4 将水面从 y≈0.58 下移至 ≈0.69（用户:命中偏上,构图不协调）。v1 双弧交点、v2 巡访微点版废弃。

### 网页读取｜渡面

一根线从图形边缘进入，在不可见的平面上上下穿行，偶尔被遮住又重新出现。结束时，它从另一侧带出一个很小的亮点。

### 时间｜候潮

两条不同速度的轨迹绕同一个偏心点运行，但不构成钟面。每轮只有一次重合，重合瞬间闪过一点暖金，然后再次错开。

### 计算｜归一

三根短线以不同曲率进入，在中央经过一次交换与重排，最后合并为一条平稳的出线。表达「复杂输入得到唯一结果」，但不使用数学符号。

### 文件｜藏页

一根线折入自身形成浅浅的空间夹层，另一根细线从夹层中穿过。末尾夹层轻轻闭合，像把内容妥善收进纸里。

### 代码 / 命令｜行令

一根带有几次明确转折的线逐段被点亮；前段熄灭，后段才继续前进。末端抵达后，整条路径只亮一次，再收束为静态墨痕。

### 记忆｜回泉

两条旧轨迹以很低的透明度缓慢浮现，新线从它们之间穿过并带走一个腮红色微点。结束后旧轨迹消失，只留下新线的一小段回环。

### 未知工具｜无名流形

一条开放曲线不断改变弯曲方向，中间节点偶尔靠近却始终不组成固定形状。它表达「某种操作正在发生」，但不给出功能暗示。

## 5. 已选技术方向

优先制作一个轻量的 SwiftUI 线条动画系统：

- 使用原生 `Path` / `Shape` / `Canvas` / `TimelineView` 等 iOS 26 SwiftUI 能力。
- 用一套共享的运动语法表达生长、汇流、折返、环绕、结点与消散。
- 每个动画拥有独立轨迹和节奏，但共享线宽、圆头、颜色与渲染规则。
- 不使用 PNG 序列帧。
- 不引入 Lottie 或 Rive。
- 以 iOS 26 为最低目标；动画本身与 Liquid Glass 无关。
- 视觉概念本身保持平台无关；SwiftUI 只是当前渲染载体。

大致动效节奏曾讨论为：Thinking 每轮约 1.8–2.4 秒，工具动画每轮约 1.2–1.8 秒。该范围是初步方向，不是最终锁定值，应以实机 / 模拟器观感继续打磨。

## 6. 交付形态

私仓已提供独立的 `YushiMotionLab` SwiftUI 预览 Target：

- 与正式 `Yushi` App 的业务界面隔离。
- 能单独查看每个 20–28 pt 动画。
- 可以手动播放、暂停和重新开始，纯粹用于视觉评审。
- 不需要真实聊天消息、工具调用或网络请求来驱动。
- 不修改聊天页、Gateway、ProviderEvent、工具注册或数据模型。

私仓 `apps/ios/ci/motion-lab-preview.yml` 会被同步为公仓 `.github/workflows/motion-lab-preview.yml`，并在 macOS Runner 上：

1. 构建动画展板。
2. 启动 iOS Simulator。
3. 用 `xcrun simctl io recordVideo` 录制 MP4。
4. 用 `xcrun simctl io screenshot` 输出截图。
5. 将 MP4、截图和可选的无签名 IPA 上传为 Actions Artifacts。

这条 workflow 只服务动画视觉验收，不要混入正式 App 的发布逻辑。需要预览时，从私仓根目录运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\apps\ios\sync-to-public.ps1 `
  -Message "sync: update Motion Lab preview" `
  -PublicBranch "codex/line-animation-study"
```

## 7. 明确不做

- 不定义 Thinking 或工具动画何时出现。
- 不对接 `tool_call_started`、`tool_result` 或任何 ProviderEvent。
- 不实现工具名称到动画的业务映射表。
- 不设计聊天状态机、成功 / 失败产品逻辑或多工具排序规则。
- 不修改现有 Gateway、联系人配置、工具白名单或聊天界面。
- 不借此分支重构正式 App。
- 不把动画误做成 Liquid Glass、加载圈或通用系统图标。

如需上述能力，应在动画视觉得到用户确认后，另开集成任务处理。

## 8. 下一窗口的推荐起点

在私仓 `codex/line-animation-study` 分支继续，先阅读本文件，再编辑 `apps/ios/MotionLab/`。不要重新创建 Target、同步脚本或录制 workflow，也不要一次性制作全部九个动画。

迭代流程已验证：先在 HTML 草样（canvas，数学与 Swift 1:1）逐帧调轨迹，锁常量后移植 `MotionLab/Motion/`，再经 CI 出 MP4 给用户评审。

草样已入库（私仓 `tools/motion-sketch/`，非镜像范围，改它不会触发公仓 CI）：

```bash
node tools/motion-sketch/serve.mjs        # 打样,127.0.0.1:4173
node tools/motion-sketch/check-parity.mjs # 校验九层常量与 Swift 逐值一致
```

它直接引用 `design-refs/you_oracle_fixed_canvas_package/` 的素材原件，不存副本。移植后**跑 check-parity 比对，不要靠肉眼**。

**草样版本号会漂**：入库前它躺在会话 scratchpad 里，2026-07-20 排查时页面标题写着 v4、代码其实已是 v6 架构，害得先按 v5 的整字共享蒙版去解释串墨（推理自洽但错）。改版时 `<title>` / `<h1>` / 分节注释三处要一起改。**遇到「代码逻辑说这不可能」的矛盾，别编解释，直接跑接触表看。**

2026-07-20 进度：`Thinking｜起草` 到 v7（见 §4，第 6 笔断裂已修，轨迹改为位图反推），并按用户拍板改走 36–56 pt 大尺寸（见 §3.1）。`搜索｜拾音` 仍为 v4，未动。等待 v7 出片反馈，定稿后按同流程扩展其余七枚。

## 9. 尚未锁定、需要视觉打磨的项目

- 最终线宽及其在 20 pt、24 pt、28 pt 下的光学修正。
- 每枚动画的精确贝塞尔轨迹与线条数量。
- 点缀色分别分配给哪些动画。
- 循环是否需要无缝，以及循环间是否保留短暂停顿。
- 展板背景采用项目纸色还是同时提供浅色 / 深色对照。
- 第一轮原型选择「计算」还是「搜索」与 Thinking 配对。
- 是否需要“减少动态效果”的静态版本；这属于动画可访问性设计，但尚未由用户确认。

## 10. 给接手 Codex 的一句话

在私仓 `yushi-liuxing` 的 `codex/line-animation-study` 分支，只做一套 20–28 pt、墨线 × 雨水 × 星轨流形风格的 SwiftUI 私人动画展板；先产出 Thinking 和一枚工具动画，经公仓镜像 CI 生成预览视频，向用户确认视觉后再扩展，绝不提前接业务逻辑。
